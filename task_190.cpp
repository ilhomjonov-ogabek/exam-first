#include <iostream>
using namespace std;
int main() {
    string word;
    int num=0,num1=0,num2=0;
    cout << "Sozni kiriting:";
    getline(cin, word);
    for (int i = 0; i < word.length(); i++) {
        if (isalpha(word[i])) {
            num +=1;
        }if (isdigit(word[i])) {
            num1 +=1;
        }if (is)
    }













    return 0;
}